para acceder a phpmyadmin solo vayan a localhost:8080 o 127.0.0.1:8080
poner en la carpeta src los archivos de proyectos.
para la conexion a la base de datos el host es "mysql"

to access to phpmyadmin just go localhost:8080 or 127.0.0.1:8080
just drop on src folder your projects
the db host is "mysql"
